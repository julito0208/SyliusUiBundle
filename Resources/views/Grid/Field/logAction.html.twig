{% if data == 'create' %}
    <span class="ui primary label">
        <i class="plus icon"></i>
        {{ 'sylius.ui.created'|trans }}
    </span>
{% elseif data == 'update' %}
    <span class="ui primary label">
        <i class="pencil icon"></i>
        {{ 'sylius.ui.updated'|trans }}
    </span>
{% endif %}
